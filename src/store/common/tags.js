import { asyncRouterMap, constantRouterMap } from '@/router'

const tags = {
  state: {
    visitedViews: constantRouterMap,
    cachedViews: asyncRouterMap
  },
  mutations: {},
  actions: {}
}
export default tags
